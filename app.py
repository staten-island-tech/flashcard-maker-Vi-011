class Flashcards:
    def __init__(mode, cards, answer):
        mode.cards = cards
        mode.answer = answer
    def points(cards, answer):

answer = int(input("Answer?"))

streak = 0
for every answer = True:
    streak + 1

